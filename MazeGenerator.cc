#include "MazeGenerator.h"
#include <iostream>

/* sf::Image MazeGenerator::generate_maze(int width, int height)
{
    std::vector<bool> nodes{};

    image.create(width, height, sf::Color::Black);
    //Generate Borders
    for (int x = 0; x < width; x++)
    {
        for (int y = 0; y < height; y++)
        {
            nodes.push_back(false);
        }
    }
    int pos = rand() % nodes.size();
    nodes.at(pos) = true;
    image.setPixel((pos - y)/width)
    if ()
    {
        
    }
    pos = x * width + y

     = x
    image.saveToFile("maps/Maze.png");
} */